---
const socialLinks = [
  {
    platform: 'GitHub',
    icon: '⌨️',
    handle: '@htekdev',
    url: 'https://github.com/htekdev',
  },
  {
    platform: 'LinkedIn',
    icon: '💼',
    handle: '/in/htekdev',
    url: 'https://linkedin.com/in/htekdev',
  },
  {
    platform: 'DEV.to',
    icon: '📝',
    handle: '@htekdev',
    url: 'https://dev.to/htekdev',
  },
  {
    platform: 'YouTube',
    icon: '🎬',
    handle: '@htekdev',
    url: 'https://youtube.com/@htekdev',
  },
  {
    platform: 'NPM',
    icon: '📦',
    handle: '~hypertekdev',
    url: 'https://www.npmjs.com/~hypertekdev',
  },
  {
    platform: 'Email',
    icon: '✉️',
    handle: 'hector.flores@htek.dev',
    url: 'mailto:hector.flores@htek.dev',
  },
];
---

<section id="contact" class="relative py-24 md:py-32 bg-bg-primary overflow-hidden">
  <!-- Subtle background glow -->
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-accent-cyan/5 rounded-full blur-3xl pointer-events-none"></div>

  <div class="relative max-w-6xl mx-auto px-6">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <span class="inline-block font-mono text-accent-cyan text-sm tracking-wider mb-4">// contact</span>
      <h2 class="font-heading text-4xl md:text-5xl font-bold text-text-primary mb-4">
        Let's <span class="bg-gradient-to-r from-accent-cyan to-accent-purple bg-clip-text text-transparent">Connect</span>
      </h2>
      <p class="text-text-secondary text-lg max-w-2xl mx-auto">
        Have a project idea, want to collaborate, or just say hi?
      </p>
    </div>

    <!-- Two-column layout -->
    <div class="grid md:grid-cols-2 gap-12 items-start">
      <!-- Left: Social Links -->
      <div class="grid gap-3">
        {socialLinks.map((link) => (
          <a
            href={link.url}
            target={link.platform !== 'Email' ? '_blank' : undefined}
            rel={link.platform !== 'Email' ? 'noopener noreferrer' : undefined}
            class="contact-link group flex items-center gap-4 px-5 py-4 rounded-xl
                   bg-bg-secondary/60 backdrop-blur-sm border border-border/50
                   hover:border-accent-cyan/50 hover:bg-bg-tertiary/40
                   hover:shadow-[0_0_20px_rgba(0,229,255,0.1)]
                   transition-all duration-300"
          >
            <span class="text-2xl w-10 text-center flex-shrink-0">{link.icon}</span>
            <div class="flex flex-col min-w-0">
              <span class="text-text-primary font-medium group-hover:text-accent-cyan transition-colors duration-300">
                {link.platform}
              </span>
              <span class="text-text-tertiary text-sm font-mono truncate">
                {link.handle}
              </span>
            </div>
            <span class="ml-auto text-text-tertiary group-hover:text-accent-cyan transition-colors duration-300 flex-shrink-0">
              →
            </span>
          </a>
        ))}
      </div>

      <!-- Right: Message -->
      <div class="flex flex-col justify-center h-full">
        <div class="p-8 rounded-2xl bg-bg-secondary/40 backdrop-blur-sm border border-border/30">
          <div class="font-mono text-accent-cyan/60 text-sm mb-4">{">"} message.txt</div>
          <p class="text-text-secondary leading-relaxed text-lg">
            I'm always excited to connect with fellow builders and innovators.
            Whether you're looking to modernize your dev workflow with
            <span class="text-accent-cyan">GitHub Copilot</span>, need help with
            <span class="text-accent-purple">enterprise migrations</span>, or just
            want to geek out about <span class="text-accent-cyan">AI</span> —
            let's talk.
          </p>
          <div class="mt-6 font-mono text-text-tertiary text-sm">
            <span class="text-accent-cyan">$</span> echo "Looking forward to hearing from you" <span class="animate-pulse">▊</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
